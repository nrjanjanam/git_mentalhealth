import 'package:flutter/material.dart';

class EventsAndFestivals extends StatelessWidget {
  const EventsAndFestivals({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
